Bryan Bilek

Part Two's Codepen Link: https://codepen.io/Cubguy35/pen/WNbKpeV 

1. What is Semantic HTML? Giving meaning to or defining your content.
    2. What is HTML used for? To create pages to display on the internet.
    3. What is an attribute and where do we put it? They give extra meaning to your tags so you can manipulate what the tag does.
    4. What is the h1 tag used for? How many times should I use it on a page? The h1 tag is used to define the most important element on the page. Ideally, use it once as it defines the most important thing.
    5. Name two tags that have required attributes. input tag and select tag.
    6. What do we put in the head of our HTML document? A title, styles, meta information and more.
    7. What is an id? It specifies a unique id for an html element.
    8. What elements can I add an id to? On any element.
    9. How many times can I use the same id on a page? Just once.
    10. What is a class? Can be used to specify html elements.
    11. What elements can I add a class to? On any element.
    12. How many times can I use the same class on a page? Multiple times. 
    13. How do I get my link to open in a new tab? Add anchor tabs.
    14. What is the alt attribute used for? To provide alternative info on images or for users.
    15. How do I reference an id? Using the # symbol can reference it.
    16. What is the difference between a section and a div? A div is a less specified tag for styling or making a section whereas section tags are more specific to the content in a body.
    17. What is CSS used for? It's used to style webpages.
    18. How to we select an element? Example - every h2 on the page. You could call for the h2 tag in your code or define it with a class and call that class.
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. A class can be called by a . symbol whereas an id is called by a # symbol.
    20. How do we select classes in CSS? Write a . followed by the name of the class you want.
    21. How do we select a p element with a single class of “human””? Call the class by writing .human
    22. What is a parent child selector? When would this be useful? It's when you have an element or elements nested under a different class. Useful to call one more specifically.
    23. How do you select all links within a div with the class of sidebar? Call the class .sidebar
    24. What is a pseudo selector? It selects elements in a specific state.
    25. What do we use the change the spacing between lines? You can use the line-height element and set a value.
    26. What do we use to change the spacing between letters? Use letter-spacing property.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? Use the text-transform property.
    28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted black;
    29. How do I select everything on the page? Use the selector property (*)
    30. How do I write a comment in CSS? /* */
    31. How do I find out what file I am in, when I am using the command line? Type pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? Type ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? Type rm. It's dangerous because once you remove it, it's gone.
    34. Why should I use version control? It's useful to allow multiple people to work on something remotely.
    35. How often should I commit to github? Depends but usually when you finish each step.
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow.  

Stretch Questions

    1. What is the difference between an inline element and a block element? Formatting. Block elements break into new lines.
    2. What happens when an element is positioned absolutely? It's taken out of the flow and other elements position themselves as if it wasn't there.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default. Button, input, select. button.
    5. In your own words, explain the box model. What is the fix for the box model? It suggests for styling purposes all elements are like boxes that have margins, paddings, borders around content.